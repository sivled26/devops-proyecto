apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: devops-eks
  region: us-east-1

nodeGroups:
  - name: ng-workers
    instanceType: t3.small
    desiredCapacity: 2
    minSize: 1
    maxSize: 4
    iam:
      withAddonPolicies:
        autoScaler: true
        cloudWatch: true